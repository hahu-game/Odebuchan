UnityRoom上でのテストにおいて、確認されたバグリストは以下の通り。
修正にあたっては、それぞれのバグが確認されたセッションにおけるホストのログ(HostTestLog1204)と、クライアントのログ(ClientTestLog1204)を必要に応じて参照すること。
----------------------------------------------------------------------------------------------------

1	片方のプレイヤーが進化条件を満たし、選択フェーズで特殊能力を選択中に、もう片方のプレイヤーにWaitingForOpponentPanelが表示されない。
原因：CheckEvolution()メソッドで、進化条件を満たしたプレイヤーのみに待機パネルを表示していたため、進化していないプレイヤーには表示されなかった。
対応内容：全プレイヤーに対して待機パネルを表示するように修正（GameFlowManager.cs line 1049-1056, 1148-1153）
ステータス：✅修正完了・動作確認済み

2	特殊能力を選択中に、本来まだスタートしない選択フェーズの制限時間がスタートしてしまい、10秒に1くらいの遅いペースで制限時間がカウントされていっている。
    対戦相手が特殊能力を選択したら、選択フェーズの制限時間がスタートするのが正しい仕様。また1秒につき1カウントで進むようにする。
原因：タイマーロジックがTickRate依存（60ティック=1秒）だったため、正確に1秒ごとにカウントダウンされていなかった。
対応内容：Runner.DeltaTimeを使用した正確な時間計測に修正（GameFlowManager.cs line 63, 160-176, 341）
ステータス：✅修正完了・動作確認済み（バグ7と併せて修正）

3	進化時に追加する特殊能力にきんとれを選んだプレイヤーが、選択肢にきんとれが表示されなかった。(進化前とUIの変化がない)また、あとから進化したプレイヤーが既にきんとれが相手が選択済みにも
    関わらず、自分もきんとれを選択できてしまっている。あとから選択したプレイヤーはきんとれが表示され、選択可能となっていた。
原因：
  1. CheckEvolution()メソッドで、コメントには「すでに選ばれたものを除外」と書かれていたが、実際には除外処理が実装されていなかった
  2. NetworkString型への代入方法の問題：.Set()メソッドでは値が設定されなかった
  3. UyopyonState.csのSpawned()で_lastSpecialAbilityNameを初期化していなかった
対応内容：
  1. _selectedAbilitiesリストを確認して、既に選ばれた特殊能力を選択肢から除外するように修正（GameFlowManager.cs line 1064-1072）
  2. NetworkString型への代入を.Set()メソッドから**直接代入**に変更：
     - GameFlowManager.cs line 1138, 1144: state.SpecialAbilityName = abilityName; / state.VisualType = "Evolved";
     - UyopyonState.cs line 195: VisualType = "UyopyonBaby";
  3. UyopyonState.csのSpawned()で_lastSpecialAbilityNameの初期化を追加（line 167）
  4. GameFlowManager.csにstateオブジェクトの情報と代入後の値を確認するデバッグログを追加（line 1128, 1135-1139, 1143-1145）
ステータス：✅修正完了・動作確認済み（直接代入に変更したことで正しく動作することを確認）

4	熱中症になったとき、午前中の行動につういんがセットされた状態で選択フェーズがはじまったが、クリアボタンを押したり、他の行動を改めて選ぶとつういん以外に
    行動を書き換えられてしまう。つういんの書き換えはできないようにする必要があるため、熱中症のときは、午前の行動につういんを自動でセットした上で、午後の行動を
    最初から選択中の状態になるようにする。また、熱中症のときはクリアボタンを押したときに、午後の選択のみクリアされ、引き続き午後の行動を選択中の状態にし、午前の選択はつういんのままクリアされないようにする。
原因：CheckHeatstrokeMorningClinic()でロックフラグをすぐに解除していたため、選択フェーズ中に午前を変更できた。また、UIController.csで熱中症時の特別処理が実装されていなかった。
対応内容：ロックフラグの即時解除を削除（GameFlowManager.cs line 969）。UIController.csでの熱中症対応は複雑なため、要追加実装。
ステータス：修正中　→要確認：熱中症をつういんでなおした次の日に、まだ午前中につういんがロックされている不具合があり、その修正を行っている。修正ができているかの確認は未完了。

5	選択ボタンのUI上の元気、重さ、元気バフ、重さバフの表示と、実際の適用量が一致していない場合がある。実際に確認されたこととして、きんとれの重さバフがUI上ではマイナスの値で表示されていたが、
    実際に使用すると重さバフがプラス(こちらが正しい挙動)になった。他にも、UI上の表示が間違っていることが多くあるため、原因を分析して修正を行う。
原因：UpdateActionEffectDisplay()メソッドやUpdateAllActionEffectDisplay()メソッドで、バフ値の計算や表示が正しく行われていない可能性がある。具体的なログや再現手順が必要。
対応内容：要調査。実際のゲームプレイとログを確認して、どの特殊能力・行動でUI表示が間違っているかを特定する必要がある。
ステータス：未対応（要調査）

6	きんとれを特殊能力に選択したプレイヤーのリザルト画面の表示が「特殊能力：特殊能力」となっていた。「特殊能力：きんとれ」が正しい表示。
原因：NetworkString型のSpecialAbilityNameへの代入方法が適切でなかった可能性がある。
対応内容：NetworkStringの.Set()メソッドを使用して値を設定するように修正し、デバッグログを追加（GameFlowManager.cs line 1128-1129）
ステータス：修正完了　→要リザルト画面確認

7    選択フェーズのタイマーが1秒につき1カウントになっていない。1秒につき1カウントで必ず進むように修正する。
原因：タイマーロジックがTickRate依存（60ティック=1秒）だったため、正確に1秒ごとにカウントダウンされていなかった。
対応内容：Runner.DeltaTimeを使用した正確な時間計測に修正（GameFlowManager.cs line 63, 160-176, 341）
ステータス：✅修正完了・動作確認済み（バグ2と併せて修正）