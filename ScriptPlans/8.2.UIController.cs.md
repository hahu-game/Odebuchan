# 8.2 UIController.cs (ゲーム終了アニメーション)

## ファイルパス
`Assets/Scripts/UI/UIController.cs`

## 目的
勝利・敗北アニメーションを実装する

## 実装概要
- PlayVictoryAnimation(PlayerRef player)メソッドを追加（スタブ、後でアニメーション設定）
- PlayDefeatAnimation(PlayerRef player)メソッドを追加（スタブ、後でアニメーション設定）
- うーぴょん画像のAnimatorでアニメーション再生
- 現時点ではログ出力のみでOK

## 確認項目
- [ ] PlayVictoryAnimation()呼び出しでログが出力されること
- [ ] PlayDefeatAnimation()呼び出しでログが出力されること
- [ ] 両プレイヤーで適切なアニメーションが再生されること
